# 本地测试指南 - 快速开始

## 🚀 三种测试方案

### ✅ 方案1：使用SQLite（最简单，推荐）
**优点**：无需安装MySQL，5分钟即可开始测试
**缺点**：需要稍微修改代码

### 方案2：安装MySQL
**优点**：与生产环境一致
**缺点**：需要安装配置MySQL服务

### 方案3：使用Docker
**优点**：环境隔离，一键启动
**缺点**：需要先安装Docker Desktop

---

## 方案1：使用SQLite（推荐）

### 步骤1：安装SQLite依赖

```bash
cd backend
npm install sqlite3 --save
```

###步骤2：创建SQLite配置文件

我已经为您准备了配置文件（database-sqlite.js）

### 步骤3：修改环境变量

在 `.env` 文件中添加：
```env
USE_SQLITE=true
SQLITE_PATH=./data/ruiding.db
```

### 步骤4：初始化数据库

```bash
npm run init-db
```

### 步骤5：启动服务

```bash
npm run dev
```

### 步骤6：测试

打开浏览器访问：`http://localhost:3001/health`

---

## 方案2：安装MySQL

### macOS安装MySQL

```bash
# 使用Homebrew安装
brew install mysql

# 启动MySQL服务
brew services start mysql

# 设置root密码
mysql_secure_installation
```

### 初始化数据库

```bash
cd backend
npm run init-db
npm run dev
```

---

## 方案3：使用Docker

### 安装Docker Desktop

1. 访问：https://www.docker.com/products/docker-desktop
2. 下载并安装Docker Desktop for Mac
3. 启动Docker Desktop

### 启动MySQL容器

```bash
cd backend
docker-compose up -d
```

### 初始化数据库

```bash
npm run init-db
npm run dev
```

---

## 🧪 测试接口

### 使用curl测试

```bash
# 健康检查
curl http://localhost:3001/health

# 注册用户
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123",
    "username": "测试用户"
  }'

# 登录
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123"
  }'
```

### 使用浏览器测试

打开 `auth.html` 文件进行可视化测试

---

## ❓ 选择哪个方案？

- **快速测试**：选择方案1（SQLite）
- **完整体验**：选择方案2或3（MySQL）
- **准备部署**：选择方案2或3（MySQL）

---

## 📞 遇到问题？

常见问题：
1. **端口被占用**：修改 `.env` 中的 `PORT=3001` 为其他端口
2. **数据库连接失败**：检查数据库服务是否启动
3. **依赖安装失败**：删除 `node_modules` 重新 `npm install`

需要帮助请告诉我！
